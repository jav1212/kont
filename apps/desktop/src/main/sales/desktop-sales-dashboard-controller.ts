import{OperationContextCoordinator,type OperationContextStore}from"@kontave/operation-context-application";import{createOperationalDefaults,localDate,type OperationContextKey,type OperationalDefaults}from"@kontave/operation-context-domain";import{currency,currencyCode,exchangeRate}from"@kontave/monetary-domain";import{companyId,organizationId,userId}from"@kontave/organizations-domain";import{NativeApiClient,NativeApiFailure}from"@kontave/native-api-client";import type{NativeExchangeRateSetDto,NativeOperationalDefaultsDto,NativeSalesDashboardDto}from"@kontave/native-api-contracts";import type{DesktopAuthenticatedRequest}from"../auth/desktop-authenticated-request";import type{DesktopSalesDashboardResult}from"../../shared/desktop-api";
type ValidSalesDashboardQuery={readonly from?:string;readonly to?:string;readonly granularity:"day";readonly recentLimit:number};
export class DesktopSalesDashboardController{private readonly client:NativeApiClient;private readonly inFlight=new Map<string,Promise<DesktopSalesDashboardResult>>();constructor(baseUrl:string,request:DesktopAuthenticatedRequest){this.client=new NativeApiClient({baseUrl,client:"desktop",authenticatedFetch:(input,init)=>request.fetch(input,init)});}getDashboard(actor:unknown,organization:unknown,company:unknown,raw:unknown):Promise<DesktopSalesDashboardResult>{if(![actor,organization,company].every(v=>typeof v==="string"&&v.length>0))return Promise.resolve(failure(new Error("El contexto operativo no es válido."),"OPERATION_CONTEXT_INVALID"));const query=readQuery(raw);if(!query)return Promise.resolve(failure(new Error("El período no es válido."),"SALES_DASHBOARD_INVALID"));const requestKey=`${actor}:${organization}:${company}:${query.from??"default"}:${query.to??"default"}:${query.granularity}:${query.recentLimit}`;const current=this.inFlight.get(requestKey);if(current)return current;const operation=this.load(actor as string,organization as string,company as string,query).finally(()=>this.inFlight.delete(requestKey));this.inFlight.set(requestKey,operation);return operation;}private async load(actor:string,organization:string,company:string,query:ValidSalesDashboardQuery):Promise<DesktopSalesDashboardResult>{const key={userId:userId(actor),organizationId:organizationId(organization),companyId:companyId(company)};try{const coordinator=new OperationContextCoordinator(new ContextStore(this.client),{historical:async()=>{throw new Error("Desktop delegates exchange-rate resolution to the native API.");}},clock);await coordinator.initialize(key);const state=coordinator.getState();if(state.status!=="ready")throw state.status==="failed"?state.failure:new Error("El contexto operativo no está disponible.");const to=query.to??state.value.effectiveDate,from=query.from??`${to.slice(0,8)}01`,root=`/api/native/v1/organizations/${encodeURIComponent(organization)}/companies/${encodeURIComponent(company)}`;const parameters=new URLSearchParams({from,to,granularity:query.granularity,limit:String(query.recentLimit)});const[dashboard,rates]=await Promise.all([this.client.get<NativeSalesDashboardDto>(`${root}/sales/dashboard?${parameters.toString()}`),this.client.get<NativeExchangeRateSetDto>(`${root}/operation-context/exchange-rates?date=${encodeURIComponent(to)}`)]);return{ok:true,value:{operationContext:encode(state.value),exchangeRates:rates,dashboard}};}catch(cause){return failure(cause,"SALES_DASHBOARD_UNAVAILABLE");}}}
class ContextStore implements OperationContextStore{constructor(private readonly client:NativeApiClient){}async load(key:OperationContextKey){const dto=await this.client.get<NativeOperationalDefaultsDto>(`/api/native/v1/organizations/${encodeURIComponent(key.organizationId)}/companies/${encodeURIComponent(key.companyId)}/operation-context`);const selection=dto.exchangeRate.status==="unavailable"?{status:"unavailable"as const,effectiveDate:localDate(dto.exchangeRate.effectiveDate)}:{status:"resolved"as const,value:{rate:exchangeRate({baseCurrency:currency(dto.exchangeRate.value.baseCurrency,2),quoteCurrency:currency(dto.exchangeRate.value.quoteCurrency,2),value:dto.exchangeRate.value.value}),effectiveDate:dto.exchangeRate.value.effectiveDate,capturedAt:dto.exchangeRate.value.capturedAt,source:dto.exchangeRate.value.source}};return createOperationalDefaults({key,effectiveDate:localDate(dto.effectiveDate),presentationCurrency:currencyCode(dto.presentationCurrency),exchangeRate:selection,version:dto.version,updatedAt:dto.updatedAt});}async save():Promise<OperationalDefaults>{throw new Error("Sales dashboard does not update the operation context.");}async clear():Promise<void>{}}
const clock={now:()=>new Date().toISOString(),today:()=>localDate(new Date().toISOString().slice(0,10))};
function encode(v:OperationalDefaults):NativeOperationalDefaultsDto{return{effectiveDate:v.effectiveDate,presentationCurrency:v.presentationCurrency,exchangeRate:v.exchangeRate.status==="unavailable"?v.exchangeRate:{status:"resolved",value:{baseCurrency:v.exchangeRate.value.rate.baseCurrency.code,quoteCurrency:v.exchangeRate.value.rate.quoteCurrency.code,value:v.exchangeRate.value.rate.value,effectiveDate:v.exchangeRate.value.effectiveDate,capturedAt:v.exchangeRate.value.capturedAt,source:v.exchangeRate.value.source}},version:v.version,updatedAt:v.updatedAt};}
function readQuery(value:unknown):ValidSalesDashboardQuery|null{if(value===undefined)return{granularity:"day",recentLimit:5};if(typeof value!=="object"||value===null)return null;const q=value as{from?:unknown;to?:unknown;granularity?:unknown;recentLimit?:unknown};const granularity=q.granularity??"day",recentLimit=q.recentLimit??5;if(granularity!=="day"||!Number.isSafeInteger(recentLimit)||Number(recentLimit)<1||Number(recentLimit)>100)return null;if(q.from===undefined&&q.to===undefined)return{granularity,recentLimit:Number(recentLimit)};if(typeof q.from!=="string"||typeof q.to!=="string")return null;try{const from=localDate(q.from),to=localDate(q.to);return from<=to&&(Date.parse(`${to}T00:00:00Z`)-Date.parse(`${from}T00:00:00Z`))/86400000<=365?{from,to,granularity,recentLimit:Number(recentLimit)}:null;}catch{return null;}}
function failure(cause:unknown,fallback:string):DesktopSalesDashboardResult{let current=cause;const seen=new Set<unknown>();while(current instanceof Error&&!seen.has(current)){if(current instanceof NativeApiFailure)return{ok:false,error:{code:current.code,message:current.message,requestId:current.requestId??null}};seen.add(current);current=current.cause;}return{ok:false,error:{code:fallback,message:cause instanceof Error?cause.message:"No se pudo cargar el tablero de ventas.",requestId:null}};}
