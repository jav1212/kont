import{createClient,type SupabaseClient}from"@supabase/supabase-js";import{companyId}from"@kontave/companies-domain";import{currencyCode}from"@kontave/monetary-domain";import type{PricingContext,ProductSalePricingRepository}from"@kontave/pricing-application";import{fixedSalePricing,markupSalePricing,PricingFailure,ProductSalePricing,type SalePricingPolicy}from"@kontave/pricing-domain";import{productId,type ProductId}from"@kontave/products-domain";import{z}from"zod";
const policy=z.discriminatedUnion("mode",[z.object({mode:z.literal("fixed"),amount:z.string(),currency:z.string()}),z.object({mode:z.literal("markup"),percentage:z.string(),currency:z.string()})]);const row=z.object({companyId:z.string(),productId:z.string(),policy:policy.nullable(),version:z.number().int().positive(),updatedAt:z.string()});
export class SupabaseProductSalePricingRepository implements ProductSalePricingRepository{constructor(private readonly client:SupabaseClient){}async get(context:PricingContext,id:ProductId){const data=await this.rpc("get_native_product_sale_pricing",{...args(context),p_product_id:id});return data===null?null:map(row.parse(data));}async save(context:PricingContext,id:ProductId,value:SalePricingPolicy|null,expectedVersion:number){return map(row.parse(await this.rpc("save_native_product_sale_pricing",{...args(context),p_product_id:id,p_policy:value,p_expected_version:expectedVersion})));}private async rpc(name:string,input:Record<string,unknown>){const{data,error}=await this.client.rpc(name,input);if(error)throw failure(error);return data;}}
export function createSupabaseProductSalePricingRepository(config:{readonly url:string;readonly serviceRoleKey:string}){return new SupabaseProductSalePricingRepository(createClient(config.url,config.serviceRoleKey,{auth:{persistSession:false,autoRefreshToken:false,detectSessionInUrl:false}}));}
function args(value:PricingContext){return{p_actor_user_id:value.actorUserId,p_organization_id:value.organizationId,p_company_id:value.companyId};}function map(value:z.infer<typeof row>){const p=value.policy;return new ProductSalePricing({companyId:companyId(value.companyId),productId:productId(value.productId),policy:p===null?null:p.mode==="fixed"?fixedSalePricing(p.amount,currencyCode(p.currency)):markupSalePricing(p.percentage,currencyCode(p.currency)),version:value.version,updatedAt:value.updatedAt});}function failure(error:{message:string}){for(const code of["PRICING_VERSION_CONFLICT","PRICING_PRODUCT_NOT_FOUND","PRICING_INVALID"]as const)if(error.message.includes(code))return new PricingFailure(code,error.message,{cause:error});return new PricingFailure("PRICING_REPOSITORY_UNAVAILABLE","Pricing repository is unavailable.",{cause:error});}
