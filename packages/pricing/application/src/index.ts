import type{CompanyId}from"@kontave/companies-domain";import{PricingFailure,type ProductSalePricing,type SalePricingPolicy}from"@kontave/pricing-domain";import type{ProductId}from"@kontave/products-domain";import type{OrganizationId,UserId}from"@kontave/organizations-domain";
export interface PricingContext{readonly actorUserId:UserId;readonly organizationId:OrganizationId;readonly companyId:CompanyId}
export interface ProductSalePricingRepository{get(context:PricingContext,productId:ProductId):Promise<ProductSalePricing|null>;save(context:PricingContext,productId:ProductId,policy:SalePricingPolicy|null,expectedVersion:number):Promise<ProductSalePricing>}
export class GetProductSalePricing{constructor(private readonly repository:ProductSalePricingRepository){}async execute(context:PricingContext,productId:ProductId){const value=await this.repository.get(context,productId);if(!value)throw new PricingFailure("PRICING_PRODUCT_NOT_FOUND","Product pricing was not found.");return value;}}
export class UpdateProductSalePricing{constructor(private readonly repository:ProductSalePricingRepository){}execute(context:PricingContext,productId:ProductId,policy:SalePricingPolicy|null,expectedVersion:number){if(!Number.isSafeInteger(expectedVersion)||expectedVersion<1)throw new PricingFailure("PRICING_INVALID","Expected pricing version is invalid.");return this.repository.save(context,productId,policy,expectedVersion);}}
