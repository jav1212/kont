import assert from "node:assert/strict";import test from "node:test";
import{CreateInventoryOperation,ListInventoryFlows,PostInventoryOperation,ReverseInventoryOperation,type InventoryOperationsRepository}from"../src/inventory-operations";
const repository={list:async()=>({items:[],nextCursor:null,total:0,summary:{movementCount:0,totalValue:{amount:"0",currency:"VES"},quantities:[]}}),get:async()=>{throw new Error()},create:async input=>input as never,update:async input=>input as never,post:async input=>input as never,reverse:async input=>input as never} satisfies InventoryOperationsRepository;
const context={actorUserId:"00000000-0000-4000-8000-000000000001" as never,organizationId:"00000000-0000-4000-8000-000000000002" as never,companyId:"J-1" as never};
test("flow query rejects inverted periods",()=>assert.throws(()=>new ListInventoryFlows(repository).execute({...context,from:"2026-08-02",to:"2026-08-01",limit:25}),/period is invalid/));
test("manual capabilities cannot create purchase receipts",()=>assert.throws(()=>new CreateInventoryOperation(repository).execute({...context,reason:"purchase_receipt",effectiveDate:"2026-08-16",lines:[{productId:"p",direction:"inbound",quantity:"1",unit:"each" as never}]}),/owning capability/));
test("self consumption must be outbound",()=>assert.throws(()=>new CreateInventoryOperation(repository).execute({...context,reason:"self_consumption",effectiveDate:"2026-08-16",lines:[{productId:"p",direction:"inbound",quantity:"1",unit:"each" as never}]}),/direction/));
test("post and reverse require optimistic versions",()=>{assert.throws(()=>new PostInventoryOperation(repository).execute({...context,operationId:"o",expectedVersion:0}),/expectedVersion/);assert.throws(()=>new ReverseInventoryOperation(repository).execute({...context,operationId:"o",expectedVersion:1,effectiveDate:"2026-08-16",reason:" "}),/reason is required/);});
