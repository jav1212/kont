{
  "name": "@kontave/devices-testing",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": { ".": { "types": "./src/index.ts", "default": "./dist/index.js" } },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "check": "tsc --noEmit",
    "test": "node -e \"console.log('Test fixtures validated through devices-core')\""
  },
  "files": ["dist"],
  "dependencies": {
    "@kontave/device-contracts": "workspace:*",
    "@kontave/devices-core": "workspace:*"
  },
  "devDependencies": { "typescript": "^5.9.2" }
}
