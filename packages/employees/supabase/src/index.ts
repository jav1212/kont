import{createClient,type SupabaseClient}from"@supabase/supabase-js";import{companyId,type CompanyId}from"@kontave/companies-domain";import type{CompensationHistoryRepository,EmployeeRepository}from"@kontave/employees-application";import{Employee,EmployeeFailure,employeeId,nationalId,type Compensation,type EmployeeId,type NationalId}from"@kontave/employees-domain";import{employeeRowSchema}from"./persistence-codecs";
export function createEmployeeInfrastructure(configuration:{readonly url:string;readonly serviceRoleKey:string}){const client=createClient(configuration.url,configuration.serviceRoleKey,{auth:{persistSession:false,autoRefreshToken:false,detectSessionInUrl:false}});return{employees:new SupabaseEmployeeRepository(client),compensations:new SupabaseCompensationHistory(client)}}
class SupabaseEmployeeRepository implements EmployeeRepository{constructor(private readonly client:SupabaseClient){}async list(id:CompanyId){const{data,error}=await this.client.rpc("list_company_employees",{p_company_id:id});if(error)throw repositoryFailure(error);return employeeRowSchema.array().parse(data??[]).map(mapEmployee)}async findById(company:CompanyId,id:EmployeeId){return(await this.list(company)).find(e=>e.id===id)??null}async findByNationalId(company:CompanyId,id:NationalId){return(await this.list(company)).find(e=>e.person.nationalId===id)??null}async save(employee:Employee){const{error}=await this.client.rpc("save_operational_employee",{p_employee_id:employee.id,p_company_id:employee.companyId,p_legacy_employee_id:employee.legacyEmployeeId,p_national_id:employee.person.nationalId,p_full_name:employee.person.fullName,p_position:employee.employment.position,p_hired_on:employee.employment.hiredOn,p_employment_type:employee.employment.type,p_terminated_on:employee.employment.terminatedOn,p_status:employee.status,p_version:employee.version});if(error)throw repositoryFailure(error)}}
class SupabaseCompensationHistory implements CompensationHistoryRepository{constructor(private readonly client:SupabaseClient){}async replaceCurrent(employee:Employee,_previous:Compensation|null,reason:string){const{error}=await this.client.rpc("change_operational_employee_compensation",{p_employee_id:employee.id,p_monthly_salary_minor:employee.compensation.monthlySalaryMinor.toString(),p_currency:employee.compensation.currency,p_effective_from:employee.compensation.effectiveFrom,p_reason:reason});if(error)throw repositoryFailure(error)}}
function mapEmployee(row:ReturnType<typeof employeeRowSchema.parse>){return new Employee({id:employeeId(row.id),companyId:companyId(row.company_id),legacyEmployeeId:row.legacy_employee_id,person:{nationalId:nationalId(row.national_id),fullName:row.full_name},employment:{position:row.position,hiredOn:row.hired_on,type:row.employment_type,terminatedOn:row.terminated_on},compensation:{monthlySalaryMinor:BigInt(row.monthly_salary_minor),currency:row.currency,effectiveFrom:row.effective_from},status:row.status,version:row.version})}function repositoryFailure(cause:unknown){return new EmployeeFailure("EMPLOYEE_REPOSITORY_UNAVAILABLE","No se pudo acceder a los empleados.",{cause})}
