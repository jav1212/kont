import { Catalog } from "./ui-catalog";

/**
 * Exercises the server-to-client boundary of the unified UI distribution.
 * @returns The interactive UI fixture rendered through a server component.
 */
export default function Page() {
  return <Catalog />;
}
