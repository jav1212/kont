import{productId}from"@kontave/products-domain";import{createProductCommercialActions}from"@/src/native-api/v1/products/product-commercial-actions";import{executeProductRequest,productsRead}from"@/src/native-api/v1/products/product-http";type C={params:Promise<{organizationId:string;companyId:string;productId:string}>};export const dynamic="force-dynamic";
export async function GET(request:Request,c:C){const p=await c.params,url=new URL(request.url),to=url.searchParams.get("to")??caracasDate(),from=url.searchParams.get("from")??yearBefore(to),granularity=url.searchParams.get("granularity")??"day";return executeProductRequest(request,p.organizationId,p.companyId,productsRead,async(_a,context)=>createProductCommercialActions().unitEconomics.execute({...context,productId:productId(p.productId),from,to,granularity:granularity as"day"|"week"|"month"}));}
function caracasDate(){return new Intl.DateTimeFormat("en-CA",{timeZone:"America/Caracas",year:"numeric",month:"2-digit",day:"2-digit"}).format(new Date());}function yearBefore(value:string){const date=new Date(`${value}T12:00:00Z`);date.setUTCFullYear(date.getUTCFullYear()-1);date.setUTCDate(date.getUTCDate()+1);return date.toISOString().slice(0,10);}
