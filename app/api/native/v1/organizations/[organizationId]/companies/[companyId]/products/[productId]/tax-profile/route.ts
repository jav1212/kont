import type{NativeUpdateProductTaxationDto}from"@kontave/native-api-contracts";import{productId}from"@kontave/products-domain";import type{ResolvedProductTaxation}from"@kontave/taxation-application";import{VENEZUELAN_IVA}from"@kontave/taxation-venezuela";import{createProductCommercialActions}from"@/src/native-api/v1/products/product-commercial-actions";import{executeProductRequest,productsRead,productsUpdate}from"@/src/native-api/v1/products/product-http";type C={params:Promise<{organizationId:string;companyId:string;productId:string}>};
export async function GET(request:Request,c:C){const p=await c.params,url=new URL(request.url),date=url.searchParams.get("effectiveDate")??caracasDate();return executeProductRequest(request,p.organizationId,p.companyId,productsRead,async(_a,context)=>dto(await createProductCommercialActions().getTaxation.execute(context,productId(p.productId),VENEZUELAN_IVA,date)));}
export async function PATCH(request:Request,c:C){const p=await c.params,b=await request.json()as NativeUpdateProductTaxationDto;return executeProductRequest(request,p.organizationId,p.companyId,productsUpdate,async(_a,context)=>{const actions=createProductCommercialActions(),id=productId(p.productId);await actions.updateTaxation.execute({...context,productId:id,taxCode:VENEZUELAN_IVA,treatment:b.treatment,effectiveFrom:b.effectiveFrom,legalBasis:b.legalBasis,expectedVersion:b.expectedVersion});return dto(await actions.getTaxation.execute(context,id,VENEZUELAN_IVA,b.effectiveFrom));});}
function dto(value:ResolvedProductTaxation){return{profileId:value.profile.id,taxCode:value.assignment.taxCode,treatment:value.assignment.treatment,effectiveFrom:value.assignment.effectiveFrom,effectiveTo:value.assignment.effectiveTo,resolvedRate:value.rule.rate,legalBasis:value.assignment.legalBasis,ruleVersion:`${value.assignment.classificationVersion}|${value.rule.version}`,version:value.profile.version};}function caracasDate(){return new Intl.DateTimeFormat("en-CA",{timeZone:"America/Caracas",year:"numeric",month:"2-digit",day:"2-digit"}).format(new Date());}
