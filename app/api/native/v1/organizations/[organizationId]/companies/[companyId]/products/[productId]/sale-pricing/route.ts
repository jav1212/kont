import type{NativeUpdateProductSalePricingDto}from"@kontave/native-api-contracts";import{currencyCode}from"@kontave/monetary-domain";import{fixedSalePricing,markupSalePricing,PricingFailure,type ProductSalePricing}from"@kontave/pricing-domain";import{productId}from"@kontave/products-domain";import{createProductCommercialActions}from"@/src/native-api/v1/products/product-commercial-actions";import{executeProductRequest,productsRead,productsUpdate}from"@/src/native-api/v1/products/product-http";type C={params:Promise<{organizationId:string;companyId:string;productId:string}>};
export async function GET(request:Request,c:C){const p=await c.params;return executeProductRequest(request,p.organizationId,p.companyId,productsRead,async(_a,context)=>dto(await createProductCommercialActions().getPricing.execute(context,productId(p.productId))));}
export async function PATCH(request:Request,c:C){const p=await c.params,b=await request.json()as NativeUpdateProductSalePricingDto;return executeProductRequest(request,p.organizationId,p.companyId,productsUpdate,async(_a,context)=>dto(await createProductCommercialActions().updatePricing.execute(context,productId(p.productId),policy(b.policy),b.expectedVersion)));}
function policy(value:NativeUpdateProductSalePricingDto["policy"]){if(value===null)return null;if(value.mode==="fixed")return fixedSalePricing(value.amount,currencyCode(value.currency));if(value.mode==="markup")return markupSalePricing(value.percentage,currencyCode(value.currency));throw new PricingFailure("PRICING_INVALID","Pricing mode is invalid.");}function dto(value:ProductSalePricing){return{policy:value.policy,version:value.version,updatedAt:value.updatedAt};}
