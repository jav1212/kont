import{productId}from"@kontave/products-domain";import{executeProductRequest,productsRead}from"@/src/native-api/v1/products/product-http";type C={params:Promise<{organizationId:string;companyId:string;productId:string}>};export async function GET(request:Request,c:C){const p=await c.params,u=new URL(request.url),limit=Number(u.searchParams.get("limit")??25);return executeProductRequest(request,p.organizationId,p.companyId,productsRead,(a,context)=>a.movements.execute({...context,productId:productId(p.productId),cursor:u.searchParams.get("cursor"),limit,from:u.searchParams.get("from"),to:u.searchParams.get("to"),type:u.searchParams.get("type"),locationId:u.searchParams.get("locationId")}))}
