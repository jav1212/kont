// Layout pass-through for the experimental guided payroll wizard.
// Inherits CompanyProvider from the (app) layout above, just like /payroll.
export default function PayrollGuidedLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
