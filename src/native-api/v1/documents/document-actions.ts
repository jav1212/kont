import { CreateDocumentFolder,CreateDocumentUpload,DeleteDocument,DeleteDocumentFolder,GetDocumentDownload,ListDocumentFolders,ListDocuments,MoveDocument,RegisterDocument,RenameDocumentFolder } from "@kontave/documents-application";
import { createDocumentsInfrastructure } from "@kontave/documents-supabase";

export function createDocumentActions(){const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!url||!key)throw new Error("Native documents are not configured.");const infrastructure=createDocumentsInfrastructure({url,serviceRoleKey:key}),repository=infrastructure.repository,storage=infrastructure.storage;return{listDocuments:new ListDocuments(repository),listFolders:new ListDocumentFolders(repository),createFolder:new CreateDocumentFolder(repository),renameFolder:new RenameDocumentFolder(repository),deleteFolder:new DeleteDocumentFolder(repository),register:new RegisterDocument(repository),move:new MoveDocument(repository),createUpload:new CreateDocumentUpload(storage),download:new GetDocumentDownload(repository,storage),deleteDocument:new DeleteDocument(repository,storage)}}
