import { CreateProduct,CreateProductCategory,GetProduct,ListProductCategories,ListProductMovements,ListProducts,SetProductCategoryStatus,SetProductStatus,UpdateProduct,UpdateProductCategory } from "@kontave/products-application";
import { ProductCategoryStatus,ProductStatus } from "@kontave/products-domain";
import { createSupabaseProductsRepository } from "@kontave/products-supabase";
export function createProductActions(){const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!url||!key)throw new Error("Native products infrastructure is not configured.");const repository=createSupabaseProductsRepository({url,serviceRoleKey:key});return{list:new ListProducts(repository),get:new GetProduct(repository),create:new CreateProduct(repository),update:new UpdateProduct(repository),activate:new SetProductStatus(repository,ProductStatus.Active),deactivate:new SetProductStatus(repository,ProductStatus.Inactive),movements:new ListProductMovements(repository),listCategories:new ListProductCategories(repository),createCategory:new CreateProductCategory(repository),updateCategory:new UpdateProductCategory(repository),activateCategory:new SetProductCategoryStatus(repository,ProductCategoryStatus.Active),deactivateCategory:new SetProductCategoryStatus(repository,ProductCategoryStatus.Inactive)};}
