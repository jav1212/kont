import { ArchiveOrganizationRole, CreateOrganizationRole, ListOrganizationRoles, ListPermissions, UpdateOrganizationRole } from "@kontave/access-control-application";
import { createSupabaseAuthorization } from "@kontave/access-control-supabase";
export function createAccessControlActions(){const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!url||!key)throw new Error("Access Control is not configured.");const infrastructure=createSupabaseAuthorization({url,serviceRoleKey:key});return{...infrastructure,listPermissions:new ListPermissions(infrastructure.administration),listRoles:new ListOrganizationRoles(infrastructure.administration),createRole:new CreateOrganizationRole(infrastructure.administration),updateRole:new UpdateOrganizationRole(infrastructure.administration),archiveRole:new ArchiveOrganizationRole(infrastructure.administration)};}
