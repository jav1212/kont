import { AccessControlFailure, AuthorizationDenied, PERMISSIONS, permissionCode, type PermissionCode, type Role } from "@kontave/access-control-domain";
import type { NativeRoleDto } from "@kontave/native-api-contracts";
import { authenticateNativeRequest } from "../auth/native-auth-context";import{nativeClientSource}from"../http/native-client-source";import{nativeError,nativeSuccess}from"../http/native-response";import{createAccessControlActions}from"./access-control-actions";
export const toRoleDto=(r:Role):NativeRoleDto=>({id:r.id,organizationId:r.organizationId!,code:r.code,name:r.name,description:r.description,kind:r.kind,permissions:r.permissions,status:r.status,version:r.version});
export async function executeRoleRequest<T>(request:Request,organizationId:string,permission:PermissionCode,operation:(actions:ReturnType<typeof createAccessControlActions>,snapshot:NonNullable<Awaited<ReturnType<ReturnType<typeof createAccessControlActions>["repository"]["findSnapshot"]>>>)=>Promise<T>){const requestId=crypto.randomUUID();try{const identity=await authenticateNativeRequest(request);if(!identity)return nativeError("INVALID_ACCESS_TOKEN","La sesión no es válida o expiró.",requestId,401);const actions=createAccessControlActions();await actions.require.execute({actor:{userId:identity.userId,organizationId},permission,resource:{type:"roles",organizationId},context:{requestId,source:nativeClientSource(request.headers.get("x-kontave-client")),occurredAt:new Date().toISOString()}});const snapshot=await actions.repository.findSnapshot(identity.userId,organizationId);if(!snapshot)return nativeError("ORGANIZATION_ACCESS_DENIED","No tienes acceso a los roles.",requestId,403);return nativeSuccess(await operation(actions,snapshot),requestId);}catch(cause){if(cause instanceof AuthorizationDenied)return nativeError("ORGANIZATION_ACCESS_DENIED","No tienes acceso a los roles.",requestId,403);if(cause instanceof AccessControlFailure){const status=cause.code==="ROLE_NOT_FOUND"?404:cause.code==="ROLE_VERSION_CONFLICT"||cause.code==="ROLE_IN_USE"?409:cause.code==="ACCESS_CONTROL_REPOSITORY_UNAVAILABLE"?503:400;return nativeError(cause.code,cause.message,requestId,status);}console.error("native.roles.failed",{requestId,cause});return nativeError("INTERNAL_ERROR","No se pudieron procesar los roles.",requestId,500);}}
export const rolesRead=permissionCode(PERMISSIONS.ROLES_READ),rolesManage=permissionCode(PERMISSIONS.ROLES_MANAGE);
