import { AuthorizationDenied, PERMISSIONS, permissionCode, type PermissionCode } from "@kontave/access-control-domain";
import { createSupabaseAuthorization } from "@kontave/access-control-supabase";
import { companyId } from "@kontave/companies-domain";
import { InventoryFailure } from "@kontave/inventory-domain";
import { RequireCompanyModuleCapability } from "@kontave/modules-application";
import { ModuleCapability, ModuleFailure } from "@kontave/modules-domain";
import { createModulesInfrastructure } from "@kontave/modules-supabase";
import { organizationId, userId } from "@kontave/organizations-domain";
import { OrganizationAccessFailure, OrganizationAccessPathKind } from "@kontave/organization-delegations-domain";
import { DelegatedPermissionScopePolicy } from "@kontave/workspace-context-application";
import type { NativeApiErrorCode } from "@kontave/native-api-contracts";
import { authenticateNativeRequest } from "../auth/native-auth-context";
import { createCompanyActions } from "../companies/company-actions";
import { nativeClientSource } from "../http/native-client-source";
import { nativeError, nativeSuccess } from "../http/native-response";
import { createOrganizationAccessActions } from "../organization-access/organization-access-actions";
import { createInventoryOperationActions } from "./inventory-operation-actions";

export const inventoryRead=permissionCode(PERMISSIONS.INVENTORY_READ),inventoryCreate=permissionCode(PERMISSIONS.INVENTORY_CREATE),inventoryUpdate=permissionCode(PERMISSIONS.INVENTORY_UPDATE);
type Actions=ReturnType<typeof createInventoryOperationActions>;
export async function executeInventoryOperationRequest<T>(request:Request,rawOrganizationId:string,rawCompanyId:string,permission:PermissionCode,operation:(actions:Actions,context:{actorUserId:ReturnType<typeof userId>;organizationId:ReturnType<typeof organizationId>;companyId:ReturnType<typeof companyId>})=>Promise<T>):Promise<Response>{const requestId=crypto.randomUUID();try{const identity=await authenticateNativeRequest(request);if(!identity)return nativeError("INVALID_ACCESS_TOKEN","La sesión no es válida o expiró.",requestId,401);const organization=organizationId(rawOrganizationId),company=companyId(rawCompanyId),actor=userId(identity.userId),occurredAt=new Date().toISOString();const access=(await createOrganizationAccessActions().portfolio.execute(actor,occurredAt)).find(item=>item.organizationId===organization);if(!access||!new DelegatedPermissionScopePolicy().permits(access.accessPath,permission))return nativeError("INVENTORY_OPERATION_ACCESS_DENIED","No tienes acceso a las operaciones de inventario.",requestId,403);const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!url||!key)throw new Error("Native inventory operations infrastructure is not configured.");if(access.accessPath.kind===OrganizationAccessPathKind.DirectMembership)await createSupabaseAuthorization({url,serviceRoleKey:key}).require.execute({actor:{userId:identity.userId,organizationId:organization},permission,resource:{type:"inventory_operations",organizationId:organization,companyId:company},context:{requestId,source:nativeClientSource(request.headers.get("x-kontave-client")),occurredAt}});await createCompanyActions().getOperational.execute(organization,company);const modules=createModulesInfrastructure({url,serviceRoleKey:key});await new RequireCompanyModuleCapability(modules.catalog,modules.companyActivations).execute(organization,company,ModuleCapability.InventoryMovements);return nativeSuccess(await operation(createInventoryOperationActions(),{actorUserId:actor,organizationId:organization,companyId:company}),requestId);}catch(cause){if(cause instanceof InventoryFailure){const status=cause.code==="INVENTORY_OPERATION_NOT_FOUND"?404:cause.code==="INVENTORY_OPERATION_VERSION_CONFLICT"||cause.code==="INVENTORY_OPERATION_TRANSITION_INVALID"||cause.code==="INVENTORY_PERIOD_CLOSED"||cause.code==="INVENTORY_NEGATIVE_STOCK"?409:cause.code==="INVENTORY_OPERATION_ACCESS_DENIED"?403:cause.code==="INVENTORY_REPOSITORY_UNAVAILABLE"?503:400;return nativeError(inventoryOperationErrorCode(cause),cause.message,requestId,status);}if(cause instanceof AuthorizationDenied||cause instanceof OrganizationAccessFailure)return nativeError("INVENTORY_OPERATION_ACCESS_DENIED","No tienes acceso a las operaciones de inventario.",requestId,403);if(cause instanceof ModuleFailure)return nativeError(cause.code,cause.message,requestId,409);console.error("native.inventory_operations.failed",{requestId,cause});return nativeError("INTERNAL_ERROR","No se pudo procesar la operación de inventario.",requestId,500);}}

function inventoryOperationErrorCode(failure:InventoryFailure):NativeApiErrorCode{switch(failure.code){case"INVENTORY_OPERATION_NOT_FOUND":case"INVENTORY_OPERATION_VERSION_CONFLICT":case"INVENTORY_OPERATION_TRANSITION_INVALID":case"INVENTORY_OPERATION_ACCESS_DENIED":case"INVENTORY_PERIOD_CLOSED":case"INVENTORY_NEGATIVE_STOCK":case"INVENTORY_REPOSITORY_UNAVAILABLE":return failure.code;default:return"INVENTORY_OPERATION_INVALID";}}

export function inventoryFlowQuery(request:Request,direction?:"inbound"|"outbound"){const p=new URL(request.url).searchParams,from=p.get("from"),to=p.get("to");if(!from||!to)throw new InventoryFailure("INVENTORY_OPERATION_INVALID","from y to son obligatorios.");return{from,to,direction,reason:(p.get("reason")??undefined) as never,sourceKind:(p.get("sourceKind")??undefined) as never,productId:p.get("productId")??undefined,status:(p.get("status")??undefined) as never,search:p.get("search")??undefined,cursor:p.get("cursor")??undefined,limit:Number(p.get("limit")??25)};}
export async function readJson(request:Request):Promise<Record<string,unknown>>{try{return await request.json() as Record<string,unknown>;}catch{throw new InventoryFailure("INVENTORY_OPERATION_INVALID","El cuerpo JSON no es válido.");}}
