import{UpdateReplenishmentPolicy}from"@kontave/inventory-application";import{createSupabaseReplenishmentPolicyRepository}from"@kontave/inventory-supabase";export function createReplenishmentActions(){const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!url||!key)throw new Error("Native replenishment infrastructure is not configured.");return{update:new UpdateReplenishmentPolicy(createSupabaseReplenishmentPolicyRepository({url,serviceRoleKey:key}))};}
